spring:
  application:
    name: spring-security-bouali
  datasource:
    url: jdbc:postgres://localhost:5432/spring_security
    username: postgres
    password: chris
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    properties:
      hibernate:
        format-sql: true
app:
  security:
